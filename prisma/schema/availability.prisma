model Availability {
  id           String   @id @default(uuid())
  technicianId String
  technician   TechnicianProfile @relation(fields: [technicianId], references: [id])
  dayOfWeek    Int      
  startTime    String   
  endTime      String   

  
@@map("availabilities")
}
